# Ollama Installation

```bash
sudo apt update                          # refresh package lists
curl -fsSL https://ollama.com/install.sh | sh   # download and run official install script
ollama --version                         # confirm install, check version
ollama pull llama3                       # download a model to run locally
ollama run llama3                        # start the model in an interactive session
```
